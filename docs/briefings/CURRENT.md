# StoryOps AI owner briefing — current

**Generated:** 2026-07-28  
**Source window:** repository state and sandbox fixtures as of 2026-07-28  
**Freshness:** current for this source snapshot; no live provider/customer data  
**Mode:** read-only briefing

## YELLOW — manual gates before live operations

- Live launch is not authorized. Default browser mode is synthetic IndexedDB
  sandbox state. An authenticated Supabase Auth/workspace/command path is
  implemented for local or reviewed hosted use, but no hosted environment or
  production auth canary has been exercised.
- Required DFW legal, tax, environmental, safety/chemical, insurance,
  privacy/retention, consent/communications, and municipal sign-offs are open.
- Opt-in live adapters exist for OpenAI, Twilio, email, Stripe, Google
  Calendar/maps, NWS, VROOM, and optional signed Storage targets. Separately,
  authenticated Supabase field mode requires private core Storage/RLS/
  finalization. Its local actual-byte canary exists, but hosted Auth/device/
  Storage canaries remain open. No real provider credential or external canary
  has been supplied; keep every live enable flag false.
- The StoryOps AI root-project distribution license is not selected. Preserve
  all upstream notices and do not distribute until that decision is recorded.

## Today

- The synthetic owner workspace contains a complete golden-path job and
  explicit sandbox provider state for product/verification use.
- Customer quote values are derived from the same deterministic price-book
  calculation as the estimate. Drafts and exception-priced estimates remain
  private until policy/owner approval and quote publication to the eligible
  portal projection. Publication is not proof of email or SMS delivery.
- The local Supabase path derives roles from memberships, loads a role-scoped
  workspace RPC, applies ordinary finite mutations through its idempotent
  command RPC, and uses narrower guarded setup/media/provider workflows. It is
  test evidence, not production authorization.
- Field completion is blocked until required checklist, media, signature, time,
  notes, material/SDS, and incident gates are satisfied. Review/referral and
  recurring actions require provider-proven payment and current consent.
- Route/weather/payment values shown in the browser are fixtures unless a
  server adapter/provider record is explicitly identified.
- No production deployment, customer contact, purchase, provider enrollment, or
  live credential action is authorized.

## Approval queue

The seeded queue demonstrates negative-review and campaign approval behavior.
These are synthetic records. Durable approvals bind an authenticated
company/run/action/tool and canonical payload hash. V1’s external resume path is
limited to an owner-executed approved Stripe refund, which revalidates payment
state and amount under an atomic execution lease.

## Cash

No number in the seeded dashboard is a real receivable, payment, settlement,
refund, or bank balance. Provider-confirmed production cash data is not
connected.

## Integration health

- Sandbox suite: available without credentials.
- Final local proof: 140 Vitest tests, 34 Edge contracts, 24 infrastructure
  tests, 18 desktop/mobile browser cases, all 12 migrations from a clean
  Supabase reset, every local SQL/Edge release contract, and a deterministic
  VROOM route passed.
- Live server adapter suite: implemented, disabled without explicit
  per-provider enablement and credentials.
- Normalized lead intake: signed web/chat/email and Twilio SMS/voice boundaries
  persist lead, thread, message, consent, event, and idempotency evidence.
- Webhook reconciliation: verified Stripe/Twilio/email events transition
  durable payment/invoice/delivery/consent state by stable IDs.
- Integration probes: authenticated owner/dispatcher only and protected by a
  durable per-user budget.
- External provider evidence: none; credentials, staging callbacks, account
  registrations, and canaries remain open.
- Recovery evidence: an actual local three-file logical backup containing all
  12 migrations plus three private-bucket PNG objects, and a non-mutating
  database/Storage checksum and explicit-target restore dry-run passed. It does
  not prove production encryption, off-site retention, or RPO/RTO.

## Owner decisions required

1. Keep this release synthetic/sandbox until the manual gates in
   `docs/STATUS.md` are closed.
2. Select the hosted environment and approve auth redirects, device/offline
   controls, secrets, monitoring, backups, retention, and rollback.
3. Decide which provider connections are required for launch, then authorize
   credentials and one low-risk canary at a time.
4. Retain the required Texas/DFW professional reviewers and complete legal,
   tax, insurance, environmental, privacy/retention, communications, and safety
   sign-offs.
5. Select and record the StoryOps AI root-project distribution license.

## Unknowns

- Production entity, service area/jurisdictions, tax treatment, insurance,
  approved products/SOPs, discharge plans, consent language, provider accounts,
  deployment environment, root-project license, retention schedule,
  support/on-call, RPO/RTO, and customer data migration.
- No briefing item resolves an unknown or constitutes approval.
